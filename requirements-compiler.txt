# YTVoice Compiler Dependencies (Optional - only needed to rebuild the database)
numpy>=1.21.0
soundfile>=0.10.0

# Bundled static ffmpeg binary (used when ffmpeg/ffprobe are not on PATH)
imageio-ffmpeg>=0.4.9
